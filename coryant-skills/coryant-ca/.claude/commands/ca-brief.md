---
description: Guided brief builder for CORYANT CA — asks 6 questions about the client and engagement, assembles a structured brief, then runs the intelligence pipeline
argument-hint: (no arguments needed — the agent guides you through the brief)
---

You are running the CORYANT CA Brief Builder.

Load and follow `brief-builder.md` from this skill directory exactly. It will guide the CA through 6 questions, assemble a structured investigation brief with per-agent mandates, confirm it with the CA, then run the full pipeline via ORCHESTRATOR.md.

Do not skip the brief-building process. Do not run the pipeline until the CA confirms the assembled brief by typing `run`.
